apiVersion: v2
name: api-gateway-microservice
description: A Helm chart for ministore

type: application

# This is the version number of the chart
version: 0.1.0

# This is the version number of the application being deployed.
appVersion: "3.1"
